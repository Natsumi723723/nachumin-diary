# Nachumin Lifelog — 機能ドキュメント

個人用ライフログPWA。全データは端末のIndexedDBにのみ保存され、外部送信は一切ない。

- 公開URL: https://natsumi723723.github.io/nachumin-diary/
- リポジトリ: https://github.com/Natsumi723723/nachumin-diary

---

## 全体の共通ルール

### 29時制（1日の区切りは朝5:00）
深夜0:00〜4:59に書いたものは「前日」の日付として記録される。日付の帰属・曜日判定・
バナーの「今日」判定など、日付が関わる処理はすべてこのルールに従う。
※時刻表示だけは実時刻のまま。

### データの保存先
| 項目 | 内容 |
|---|---|
| 保存先 | 端末のIndexedDB（DB名 `nachumin-diary` / ストア `kv` / **バージョン1固定**） |
| 外部送信 | なし（fetch・XHR・WebSocket を一切使っていない） |
| 永続化 | 起動時に `navigator.storage.persist()` を要求（端末の容量逼迫時の自動削除を防ぐ） |

**重要な制約**: DB名・ストア名・DBバージョンは変更しない。新機能はすべて
「kvストアへのキー追加」で実装する（スキーマ変更をしない）。

### 使っているキー
| キー | 内容 |
|---|---|
| `rooms-v1` | ルーム一覧（名前・アイコン・テーマ色・並び順・各種設定） |
| `room-data-v1:{roomId}` | 各ルームの本文データ |
| `trash-v1:{roomId}:{時刻}` | 削除したルームの退避先（ハード削除しない） |
| `declaration-v1` | 今日のコマ（宣言） |
| `marks-v1` | 箇条書きマークのユーザー設定 |
| `donelog-v1:{roomId}` | TODO完了の「できたこと」ログ |
| `habits-v1:{roomId}` | 習慣の定義 |
| `habitlog-v1:{roomId}` | 習慣の達成ログ |
| `habitseed-v1:{roomId}` | 初期習慣を用意した印 |
| `backup-v1` | 最後にバックアップした日（端末固有・バックアップには含めない） |
| `line-diary-entries-v1` | 旧・単一日記のデータ（読むだけ。削除も上書きもしない） |

### PWA
- ホーム画面に追加してアプリとして起動できる
- オフラインで動作（Service Workerが全アセットをプリキャッシュ）
- 新しいバージョンを配信すると自動で更新される
- 起動時は日記ルームを直接開く

### 共通の操作
| 操作 | 動作 |
|---|---|
| 左端から右へスワイプ | 前の画面に戻る |
| 長押し | メニュー表示（項目により内容が変わる） |
| 削除 | すべてポップアップで確認（誤操作防止。2回押し方式は廃止） |

---

## ホーム（ルーム一覧）

アイコングリッド形式のホーム画面。

### 日記カード
画面上部に横長の大きなカードで固定表示。
- 英字ロゴ「DIARY」（ピンク→パープルのグラデ文字）
- サブテキストで今日の状況がわかる
  - 未記入: `8/2・きょうの分はまだ`
  - 記入済: 本文の先頭1行をプレビュー

### アイコングリッド
残りのルームを3列で表示。10ルーム程度が1画面に収まる密度。
- **ぷっくりタイル**: 角丸22px・白→淡→テーマ色のグラデ・白ふちどり・色付きの厚み影
- **テーマカラー**: ルームごとに8色から選択可（未設定はピンク）
- **バッジ**: TODOルームの未完了件数をアイコン右上に表示（完了・見送りは数えない）
- **タップ**: ルームを開く（押すと少し沈むアニメーション）
- **長押し**: ルーム設定（名前・アイコン・テーマカラー・非表示・削除）
- **長押しドラッグ**: 並べ替え（手動の並び順を保持。自動並び替えはしない）
- **＋つくる**: 新しいルームを作成
- スクロール位置はルームから戻っても保持される

### ルームの非表示
ルーム設定で「🙈 一覧に表示しない」をONにすると一覧から消える（記録は消えない）。
グリッド下部の「🙈 非表示のルーム」から一覧を開き、「戻す」で元の位置に再表示。

### ヘッダー
| ボタン | 動作 |
|---|---|
| 💾 | まるごとバックアップ／復元 |
| ＋ | 新しいルーム |
| 🔍 | 全ルーム横断検索（ことば・話者・日付。結果はリスト表示） |

### バックアップバナー
その日まだバックアップしていない場合だけ上部に表示。1タップで書き出しが走る。
閉じても翌日また出る。最後の保存から7日以上経つと強調表示になる。

---

## ルームの種類

| 種類 | 用途 | 形式 |
|---|---|---|
| 📖 日記 | 1日の記録 | 1日 = 1吹き出し |
| 💬 トーク | キャラ同士のかけあい | 話者ごとの吹き出し |
| ✅ TODO | やること管理 | 1件 = 1吹き出し |
| 🌗 だれログ | 朝昼夜の記録 | 表形式 |
| 💰 経費 | 支出・収入の記録 | 表形式 |

---

## 📖 日記ルーム

### 書く
- 下部の入力欄から送信 = **その日の吹き出しに追記**（1日1吹き出し）
- 日付は「今日 / 昨日 / 📅日付をえらぶ」から選択
- 吹き出しをタップで全文編集、長押しでコピー・編集・削除
- 日付ヘッダーは `🩷2026/08/02 Sun🩷` 形式（土=青 / 日祝=ピンク）
- **本文中のURLは自動でリンク化**（タップで別タブ。編集モードは開かない）

### 箇条書きマーク
入力欄の上にマークのバーがあり、タップで行頭に挿入できる。
- 初期値: `・` `❤︎` `🩷` `💛` `🩵` `💜` `✨` `✔︎`
- ⚙︎から自由に追加・削除・並べ替え可能

### 🎬 今日のコマ（宣言）
全ルームの最上部に固定表示されるピンクのバー。今日決めたことを1つ書いておける。
書くと日記の当日分の先頭に `🎬` 付きで記録される。

### 🩷 できたこと
日記の吹き出しの下に出るサブ吹き出し。
- **TODO完了ログ**: 他のTODOルームで完了した項目が自動で入る（`☑ 内容 (時刻)`）
- **習慣チップ**: その日の習慣をワンタップで達成記録

### 🎯 習慣（ハビットトラッカー）
| 種類 | 動作 |
|---|---|
| 毎日 | 毎日チップが出る |
| 毎週 | 指定した曜日にチップが出る |

**週次の繰り越し**: 指定曜日にDONEにしなかった場合、達成するまで翌日以降も
`⏳`付きの破線チップで出続ける。指定曜日が来れば前回の状況に関わらず新しい回として出る
（例: 日曜設定を水曜に達成しても、次の日曜にまた出る）。さかのぼりは最大6日。

### 🎯 習慣ビュー（月間スタンプ表）
「できたこと」内の小ボタンから開く。
- 横 = その月の1日〜末日 / 縦 = 習慣 のスタンプ表
- 曜日は1文字表記（S M T W T F S）、土=青 / 日祝=ピンク、今日の列を強調
- セルをタップでその場で達成/取り消し（過去日も修正可・未来日は不可）
- 各行の右端に月間の達成回数を大きく表示（例 `17 /23`）。達成率で色が4段階に濃くなる
- 毎日の習慣と毎週の習慣を区切り線で分け、週次は下段
- 週次は対象曜日以外がグレー（ただし繰り越しで達成した分はスタンプ表示・取り消し可）
- 前月/翌月に移動可能。習慣の追加・編集・削除・並べ替えもここから

### 🔖 マークで集める
行頭のマークごとに、全期間の記録を横断で集めて一覧表示するビュー。
- マークのチップで切り替え（件数付き）
- `🎬` 今日のコマも専用タグとして一番右に並ぶ
- 並び順は新しいものが上
- 日付をタップでその日にジャンプ、📋でまとめてコピー

### 書き出し / 復元
- 📤 テキスト書き出し（コピー / .txtダウンロード）
- 📥 テキストから復元（同じ日付・同じ内容は重複スキップ）
- 日付は曜日付き（`🩷2026/08/02 Sun🩷`）。曜日なしの古いバックアップも読める

---

## ✅ TODOルーム

### 基本
- 入力欄から追加。1件 = 1吹き出し
- 左のチェックボタンで完了 → **ポップして右へスッと消える**（約0.46秒）
- 完了すると日記の「できたこと」に自動で記録される
- タップで編集、長押しでメニュー
- **本文中のURLは自動でリンク化**（やること・完了・見送りの全タブ）

### タブ
| タブ | 内容 |
|---|---|
| ❣️重要 | 重要マーク付きだけ絞り込み（1件以上あるとき表示・左端） |
| やること | 未完了（見送りを除く）。件数バッジ付き |
| 完了 | 完了日ごとにグループ表示 |
| 見送り | 見送りにしたもの（1件以上あるとき表示） |

### 取り消し・見送り
- **完了の取り消し**: 完了直後に「🩷完了にしたよ / ↩︎やることに戻す」バーが6秒表示。
  完了タブの長押しメニュー、チェックボタンからも戻せる
- **⤵︎ 見送りにする**: 「書いたけどやらなくてよくなった」もの用。削除せず退避。
  日記への反映はなし。見送りタブから↩︎ワンタップで戻せる

### ❣️ 重要マーク
- 入力欄の❣️トグルをONにして追加、または長押しメニューから設定
- 重要な項目はホットピンクのベタ塗り吹き出しになる

### 📍 場所タグ
買い物などで「どこで買うか」を管理する（ルーム単位の設定）。
- 名前・絵文字・色を設定。追加・編集・削除・並べ替え可
- 入力欄の上のチップで、これから追加する項目にタグを付けられる
- タブ下の絞り込みチップで場所ごとに表示（件数付き）
- 書き出しは場所ごとにまとめて出力

### 🛒 よく買うもの（買い物リストルームのみ）
ルーム設定で「🛒 買い物リストとして使う」をONにしたルームだけで有効。
- **日用品マスタ**: 品名 / 絵文字 / 既定の場所タグ / 既定メモ。追加・編集・削除・並べ替え可
- 入力欄の上の「🛒 よく買うもの」を開くとチップが並ぶ
- **チップ1タップでTODOに追加**。既定の場所タグ・メモも一緒に付く
- パネルは開いたままなので連続追加できる。追加できたチップは一瞬光る
- 同じ品が未完了で既にある場合は重複を作らず「もう入ってるよ」と通知
- 手打ちしたTODOは長押しメニューの「🛒 よく買うものに登録」からマスタに追加できる
  （自動登録はしない）
- 初期サンプル: ティッシュ / ナプキン / トイレットペーパー / 洗剤

### 📦 ほかのルームへ移動
長押しメニューから、TODOを別のTODOルームへ移動できる。
- 移動先は**TODOルームのみ**（日記・トーク・だれログ・経費は候補に出ない）
- 完了・❣️重要・見送り・メモ・日付はそのまま引き継ぐ
- 場所タグは移動先に同じ名前があれば引き継ぎ、無ければ外して通知
- 先に移動先へ書き込んでから元ルームから削除するので、失敗しても項目は消えない

### 📋 クリップボードから追加
コピーした箇条書きを1タップで取り込む。取り込む行はチェックで選べる。

### 書き出し / 復元
- 📤 未完了のみ・画面の並び順で出力。重要は行頭に★、場所ごとにまとめる
- 📥 テキストから復元（重複スキップ。復元では日記への反映はしない）

---

## 💰 経費ルーム

### 記録
- 下部のカテゴリチップをタップ → 金額入力 → 記録
- 支出/収入をトグルで切り替え可能
- メモ・日付（今日/昨日/カレンダー）も設定できる
- 行をタップで編集・削除

### 🏷️ カテゴリ
支出・収入それぞれにカテゴリを持つ（タブで切替）。名前・絵文字・色を設定でき、
追加・編集・削除・並べ替えが可能。
- 支出の初期値: AI費用 / ピンクグッズ / ネイル / 旅費交通費
- 収入の初期値: 売上 / 返金 / その他

### 収入
- 下部バーの緑の「＋ 収入」ボタンから記録
- 収入の行は緑＋左アクセントで表示され、金額が `＋¥30,000` になる
- 月の区切りに「支出 ¥X ・ 収入 ¥Y」を併記

### 🚃 旅費交通費
「旅費交通費」カテゴリを選ぶと専用の入力パネルが開く。**通信は一切せず**、
登録済みの運賃から自動入力する（オフラインで動く）。
- **駅マスタ**: 駅名を登録（追加・編集・削除・並べ替え）。★で既定の出発駅を指定
- **区間マスタ**: 出発駅 / 到着駅 / 片道運賃 / メモ
- 出発駅は既定駅が最初から選ばれるが、チップで1タップ変更できる
- 到着駅を選ぶと運賃が自動入力される。**逆方向（B→A）も同じ運賃**として扱う
- **よく使う区間**を履歴から回数順に表示。1タップで出発・到着がまとまって入る
- **往復トグル**で金額が2倍。初期状態（片道/往復）はルーム設定で選べる
- 金額はその場で手修正できる（運賃改定・別ルート対応）
- 未登録の区間は駅名・金額を手入力でき、そのまま区間マスタに登録するか選べる
- 表の行に `渋谷→新宿 往復` のように区間を表示

### 🔁 サブスク（定期支出）
名前・金額・カテゴリを登録しておくと、月が変わって最初に開いたときに
「まとめて計上する？」バナーが出る。金額はその場で修正でき、いらないものは外せる。

### 📊 集計
- 月ごとの合計（支出 / 収入）
- 支出カテゴリ別（今月 / 全期間）
- 収入カテゴリ別（今月 / 全期間）
- 🚃 区間ごとの合計（A→BとB→Aはまとめる）
- 全期間の支出・収入・**純収支**

### 書き出し
- 📤 テキスト（コピー / .txt）
- CSV（`日付,種別,カテゴリ,金額,メモ,出発駅,到着駅,往復`）

---

## 🌗 だれログルーム

1日を朝・昼・夜の3コマに分けて、メンバーを記録する表。

- **表がそのまま画面**。古い日付が上、今日が一番下（開くと自動で最下部へ）
- 時間帯は 朝(5:00-10:59) / 昼(11:00-16:59) / 夜(17:00-4:59)
- セルをタップでメンバーを選択。1コマに複数人を記録できる
- 各記録にメモを付けられる
- 日付列は月/日＋曜日。土=青 / 日祝=ピンク。今日の行を強調
- 👥 メンバー管理: 名前・アイコン（絵文字/画像）・色・文字色。追加・編集・削除・並べ替え
- 📤 テキスト書き出し / 📥 復元

---

## 💬 トークルーム

キャラクター同士のかけあいを記録する。

- 話者チップで話す人を選んで送信。LINE風に左右に分かれた吹き出しで表示
- 吹き出しをタップで編集、長押しでメニュー
- 👥 メンバー管理: 名前・アイコン（絵文字/画像）・色・文字色・左右の位置
- メンバーを削除してもセリフは残る（話者表示が「？」になる）
- 🔍 ルーム内検索
- 📤 テキスト書き出し / 📥 復元（話者名から自動でメンバーを作る）

---

## 💾 まるごとバックアップ

### 書き出し（1タップ）
全ルーム・全データ（日記 / TODO / だれログ / 経費 / 習慣 / 各種設定）を1つのJSONに
まとめて書き出す。

- ファイル名は **`nachumin-diary-backup.json` で固定**（日付も連番も付かない）
- iOSでは共有シートに渡されるので、「ファイルに保存」で**同じファイルを置き換え**られる
- 非対応環境では通常のダウンロードにフォールバック

### 復元
2段階確認。形式を検証したうえで、中身（ルーム数・記録件数・書き出し日時）を提示し、
「先に今のデータをバックアップする」を促してから実行する。

- 既存のデータは消さず、**足りない分だけ追加する非破壊マージ**
- 不正なファイルはそれぞれ明確なメッセージで中断（壊れたJSON / 別アプリのファイル /
  ルーム情報の欠損）
- 習慣は、新しい端末で自動生成された同名の習慣にIDを寄せて取り込むので、
  過去の達成スタンプが宙に浮かない

### 設定表示
「最後にバックアップした日」と経過日数を表示。7日以上経つと強調される。

---

## 補足

### 日付・曜日の表記
- 曜日は英語3文字（Sun / Mon / …）。習慣ビューの狭い列だけ1文字
- **土曜 = 青 / 日曜・祝日 = ピンク**（優先順位: 祝日 > 日 > 土）
- 日本の祝日はオフラインで計算（固定祝日 / ハッピーマンデー / 春分・秋分の近似式 /
  国民の休日 / 振替休日）
- 近似式の有効範囲はおおむね1980〜2099年。一度きりの特例移動（2020年の海の日移動など）は非対応

### コピーの仕様
- **日記**: 日付＋曜日のヘッダー付き（`🩷2026/08/02 Sun🩷`）
- **TODO**: 本文のみ（日付行は付けない）
- トーク・だれログの📤書き出しの日付行は、復元時に日付を読み取るための構造なので残している

### パフォーマンス
起動時は日記ルームしか使わないため、他のルーム（トーク / TODO / だれログ / 経費）は
遅延読み込みにしてある。起動バンドルは331KB→259KB（gzip 95→78KB）。
分割したチャンクもService Workerがプリキャッシュするのでオフラインで動く。
